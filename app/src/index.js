import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <React.StrictMode>
    <div>Hola como esta</div>
  </React.StrictMode>,
  document.getElementById("root")
);

window.Neutralino.init();
